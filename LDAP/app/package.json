{
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/main.js",
    "start2": "node dist/users.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/express": "^4.17.3",
    "express": "^4.17.1",
    "ldapjs": "^1.0.2",
    "tslint": "^6.0.0",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/ldapjs": "^1.0.7"
  }
}
